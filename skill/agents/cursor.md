# Cursor Adapter

Invocation: run the repository CLI from Agent mode after reading `AGENTS.md` and `skill/SKILL.md`.

Capabilities: file read/write and terminal are expected; browser preview is optional.

Limit: do not edit generated atlas files by hand when the template or source model should change.

Mode: AGENTS.md Mode or Project Instruction Mode.

Cursor should read root `AGENTS.md`, then `skill/SKILL.md` for the full workflow. Treat StoryVista as a repository workflow rather than a product-specific framework.

Require manifest-bound images or semantic placeholders before generating final UI.
