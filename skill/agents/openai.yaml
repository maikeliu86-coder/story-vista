interface:
  display_name: "StoryVista"
  short_description: "Make text worlds visible"
  default_prompt: "Use $story-vista to build an evidence-aware interactive Story Atlas. Run the portable CLI when shell access exists; otherwise follow the six-step contract and keep uncertain claims unresolved."
