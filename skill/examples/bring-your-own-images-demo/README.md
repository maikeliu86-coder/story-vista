# Bring Your Own Images Demo

Use this mode when the user already has portraits, stills, screenshots, concept art, or location images.

Register each file in `image-manifest.json` with `status: "user_provided"`, then bind assets to character cards, relationship graph nodes, location cards, timelines, 3D map nodes, concept cards, and detail panels.
